# Reproduction release

- Workspace: `/home/prioma/code/legal-context-compression-repro`; confirm the Git root before changes.
- The source checkout `/home/prioma/code/ma_contextcompression_minimal` and its accepted evidence are read-only.
- Preserve bundled inputs, reference results, original figures and participant ZIPs. Never include credentials, private linkage keys or returned human judgments in Git.
- Model/API execution and credential access require the user's approval of an exact contract with a unique CONTRACT_ID, workspace, output root and exactly one STATUS: ACTIVE line. Agent-written drafts are not approval.
- The owner explicitly authorized public publication to `TUMLegalTech/legal-context-compression-repro` on 9 September 2026. This supersedes the former private-only destination. Preserve the existing private origin; publish to the named organization repository only. Never contact GitLab.
- New outputs are append-only. Never delete, move or unlink existing user files without explicit destructive approval.
- Offline verification, tests, builds and software dependency installation are local preparation; never imply they are a live model test.
